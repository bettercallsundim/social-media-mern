#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, k;
  cin >> n >> k;
  int arr[n][2] = {{8, 10}, {6, 9}, {3, 6}, {2, 5}, {1, 5}};
  for (size_t i = 0; i < n; i++) {
    cin >> arr[i][0] >> arr[i][1];
  }
  for (size_t i = 0; i < k; i++) {
    for (size_t j = 0; j < n; j++) {
      if(arr[j][0]!=-1){
        
      }
    }
  }
}